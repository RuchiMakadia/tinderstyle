//
//  RMKeychainWrapper.swift
//  tinderStyle
//
//  Created by eHeuristic on 25/02/21.
//

import Foundation

/*struct RMKeychainWrapper {
    static var token: String? {
        get {
            return KeychainWrapper.standard.string(forKey: Keys.Keychain.userToken)
        }
        set {
            if let newValue = newValue {
                KeychainWrapper.standard.set(newValue, forKey: Keys.Keychain.userToken)
            }
            else {
                KeychainWrapper.standard.removeObject(forKey: Keys.Keychain.userToken)
            }
        }
    }
}*/

